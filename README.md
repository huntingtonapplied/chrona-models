# chrona-models

Runnable inference package for chrona. `pip install chrona-models`, then `from chrona_models import load`.

Closed-source, compiled distribution. Licensed under the AHL Model EULA (see LICENSE).
